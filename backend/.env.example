# Local MongoDB via Docker (see docker-compose.yml at the repo root) — recommended default:
MONGODB_URI=mongodb://localhost:27017/eventhub

# Or MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/eventhub?retryWrites=true&w=majority

PORT=5000
